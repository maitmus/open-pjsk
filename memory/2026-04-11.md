# 2026-04-11 일일 로그

## 데일리 리뷰
- daily-review: 09:00 KST 게이트웨이 재시작으로 누락 → 09:20 수동 실행.
- daily-weather: 정상 (09:05).

## 세카이 라우터 분리 작업
**문제:** 세카이 채널에서 main 봇이 의사결정 텍스트를 직접 출력하는 버그 재발.
**원인:** sonnet 모델이 SOUL.md NO_REPLY 지시를 안정적으로 따르지 못함.

**해결책: sekai-router 에이전트 분리**
- `sekai-router` 에이전트 신설 (`workspace-sekai`, sonnet → opus로 전환)
- bindings: `discord peer=channel:1485510333115273339` → `sekai-router`
- main은 헤드쿼터 등 나머지 채널만 담당
- `daily-weather` 크론: agentId → `sekai-router`로 변경
- main 하트비트 비활성화 (every=0m), sekai-router가 하트비트 담당
- Discord `sekai-router` 봇 계정 추가 (별도 봇 토큰, `replyToMode: "off"` 설정)

**연속 발생 문제:**
1. sonnet → NO_REPLY 안 지킴 → opus 전환
2. opus → 여전히 직접 발화. 라우터 봇 계정으로 캐릭터 말투로 직접 발화.
3. 원인 파악: `message` tool 호출 시 `accountId` 없이 `message(action="send", channel="채널ID", message="...")` 형태로 호출 → 라우터 봇 계정으로 발화.
4. AGENTS.md에 올바른 호출 형식 + accountId 필수 강제 → 효과 없음.
5. 원인: `systemPromptOverride`가 설정돼 있어서 AGENTS.md/SOUL.md 전체를 대체 → 모델이 파일 못 봄.
6. `systemPromptOverride` 제거 → 재테스트 진행 중 (20:19 KST 기준 미완료).

**라우터 봇 Discord ID:** `1492335345738715187`
**라우터 봇 주의사항:**
- Message Content Intent 활성화 필요
- Send Messages 권한은 꺼져있어야 함 (라우터 봇 직접 발화 방지)
- `replyToMode: "off"`는 reply 스레드 방식만 끄는 거고 텍스트 전송 자체는 못 막음

**sekai-router 현재 상태 (20:19 KST):**
- `systemPromptOverride` 제거됨
- AGENTS.md: exec CLI 기반 대리 발화 방식으로 업데이트됨
- SOUL.md: "입이 없는 라우터, accountId 필수" 프레이밍
- 세션 초기화됨
- 게이트웨이 재시작됨
- ⚠️ 최종 테스트 결과 아직 미확인

## 게이트웨이 관련
- 게이트웨이가 09:00 전후 재시작되어 daily-review 누락. 원인 미확인 (systemd 재시작?).

## 교훈
- `systemPromptOverride`는 AGENTS.md/SOUL.md를 완전히 대체함 → 워크스페이스 파일 무시됨. 절대 사용 금지.
- `replyToMode: "off"`는 reply 스레드 방식 OFF이지 텍스트 전송 차단이 아님.
- message tool `channel` 파라미터 = Discord 플랫폼명("discord"), 채널 ID 아님. `target` 파라미터에 채널 ID.
- OpenClaw에서 에이전트 텍스트 reply를 런타임 레벨에서 완전 차단하는 공식 옵션 없음.
