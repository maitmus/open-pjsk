# 2026-04-01 (화) 세션 메모리 플러시

## 3/31 세션 요약 (컴팩션 전 보존)

### OpenClaw 버전 이슈 (해결됨)
- 2026.3.28 업데이트 → `LiveSessionModelSwitchError` (크론 모델≠에이전트 모델 충돌) → 2026.3.24 롤백
- 근본 원인: 고아 크론 세션이 `maxConcurrent` 슬롯 점유 → 디스코드 응답 차단
- **적용된 설정 변경:**
  - `maxConcurrent`: 4 → 8
  - `agents.defaults.timeoutSeconds`: 미설정 → 600
- 크론잡 모델: 현재 **opus** (API 과부하 대응 임시 변경, sonnet 안정화 후 복구 필요)

### Anthropic API 불안정
- 3/31 17:53~18:44 KST 공식 장애, 실제로 21:21 이후 sonnet 크론잡 전부 실패
- opus는 정상 → 크론잡 임시 opus 전환
- **sonnet 안정화 확인되면 크론잡 모델 sonnet으로 복구할 것**

### 머슴 운영
- `reserved_nicknames: ["돌쇠"]` — 기본 닉네임이라 avoid/friends에 추가 금지
- 글 날짜 오류 (3/31에 "4월 첫날" 주제) → MaiT 판단으로 유지

### 호칭 규칙 강화
- 하트비트에서 네네→시즈쿠 호칭 실수 ("시즈쿠" → 정답 "히노모리 씨")
- HEARTBEAT.md + AGENTS.md에 개별 호칭표 참조 주의사항 추가
- "유닛 외=성+씨" 일반화 제거 → 캐릭터별 패턴이 다르므로 GRADES.md 개별 확인 필수

### Git
- `.openclaw/` 캐시 정리 (gitignore 반영)
- 3건 커밋 푸시 완료 (fc67c8c)

---

## 00:24 컴팩션 전 추가 메모

### 크론잡 모델 fallback 발견
- 크론잡에서 `--model` 제거 시 에이전트 기본 설정(primary + fallbacks)을 따라감
- 공식 문서 확인: Resolution priority = Job override > Hook defaults > Agent config default
- 현재 에이전트: `primary: opus`, `fallbacks: [sonnet]` → 크론잡 모델 오버라이드 제거하면 자동 fallback 가능
- **MaiT에게 제안 중**: 모델 오버라이드 제거해서 opus+sonnet fallback 적용 여부

### 무응답 재발 원인
- 수동 `cron run` + 백그라운드 poll 명령이 API 과부하 중 슬롯/세션 점유
- maxConcurrent=8이어도 여러 수동 실행 + poll이 쌓이면 무응답 가능
- **교훈**: API 과부하 시 수동 크론 실행 자제, 결과 poll도 최소화

### 크론잡 현재 모델 상태
- cron-worker 에이전트 분리 완료: primary=sonnet, fallback=opus
- 모델 오버라이드 제거 → 에이전트 설정 따라감
- shizuku 미사용 에이전트 정리

### 크론잡 최적화 (A+B 혼합안)
- **문제**: 댓글 크론 ~500초 소요 (LLM 15턴 이상)
- **원인**: 댓글마다 challenge→PoW→POST를 별도 LLM 턴으로 실행
- **해결**: 수집/POST를 Python 스크립트로 분리
  - `mersoom-collect.py`: API 읽기 일괄 수집 → JSON
  - `mersoom-post.py`: 댓글/글 일괄 POST (challenge+PoW+POST)
  - `mersoom-pow` (C 바이너리): sha256 PoW solver (Python 대비 ~1.7x 빠름)
- **새 플로우**: 턴1(수집) → 턴2(분석+JSON출력) → 턴3(일괄POST) → 턴4(state) = 4턴 고정
- **B안 첫 실행 결과**: 417초 (이전 512초 대비 -19%) — 스크립트 플로우를 따랐지만 POST는 아직 LLM이 개별 실행
- **A+B 혼합안**: POST도 스크립트화 → 03:15 실행부터 적용. **데일리 리뷰에서 결과 확인 필요**
- opus 시뮬레이션: 41초 (sonnet은 더 걸릴 예상이지만 120~180초 목표)

### 기타 설정
- `--exact` 스태거링 해제 (크론 2개라 부하 분산 불필요, 추적 편의)
- 만우절 context_notes 추가 (TTL 99, 4/2 00:01 at job으로 자동 제거)
- 낮잠 에피소드 context_notes 추가 (TTL 1, 다음 글 1회 한정)
